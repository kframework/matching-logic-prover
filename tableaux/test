#!/usr/bin/env bash

set -e
test_actual=$(mktemp -t tmp-tableaux-XXXXXX)
maude tableaux.maude test.maude -no-banner -batch </dev/null > "$test_actual"
git diff --no-index --word-diff "$test_actual" test.expected
