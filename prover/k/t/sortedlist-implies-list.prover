claim \implies( \and( sortedlist(variable("H"), variable("X"), variable("K"), variable("MAX"), .Patterns), .Patterns)
              , \and( list( variable("H"), variable("X"), variable("K"), .Patterns), .Patterns )
              )
strategy kt ; ( (right-unfold-Nth(0, 0) ; simplify ; direct-proof)
              | ( simplify ; direct-proof )
              | (right-unfold-Nth(0, 1) ; simplify ; direct-proof)
              )
