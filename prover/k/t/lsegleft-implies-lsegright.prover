\implies(       \and( lsegleft (variable("H"), variable("X"), variable("Y"), variable("F"), .Patterns), .Patterns )
        , \or ( \and( lsegright(variable("H"), variable("X"), variable("Y"), variable("F"), .Patterns), .Patterns ) )
        )
