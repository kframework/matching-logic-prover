symbol isEmpty ( SetInt ) : Bool
axiom \forall { variable("S") { SetInt } }
      \iff-lfp( isEmpty( variable("S") { SetInt })
              , \or( \exists { } \and(\equals(variable("S") { SetInt } , emptyset)))
              )
claim \implies( \and(\equals(variable("S") { SetInt }, emptyset))
              , \and(isEmpty(variable("S") { SetInt }))
              )
strategy search-bound(2) 
// strategy right-unfold ; simplify ; instantiate-existentials ; smt-z3
